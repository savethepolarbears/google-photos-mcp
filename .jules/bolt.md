## 2025-05-27 - [Scope vs Performance]
**Learning:** Adding semantic mappings (e.g., "cat" -> "PETS") to leverage server-side filtering improves efficiency but alters search precision and scope. Pure performance optimizations (like HTTPS Keep-Alive) should be kept separate from logic/relevance improvements.
**Action:** separate "efficiency via logic change" from "system performance optimization".
